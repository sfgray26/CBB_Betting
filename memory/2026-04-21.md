# Session Log — 2026-04-21

**Agent:** Claude Code (Master Architect)  
**Focus:** Two-phase session — (1) Lineup/Admin repair, (2) Postman P0/P1 + Post-deploy UAT v5 remediation

---

## Phase 1: Lineup/Admin Repair (Morning Session)

### Commits
- `2749276` — Probable-pitcher fallback for lineup context
- `9147f83` — Smart lineup selector position exposure
- `80889dc` — Admin endpoint hardening
- `8ca2ebe` — Admin compatibility layer

### Files Changed
- `backend/fantasy_baseball/daily_lineup_optimizer.py` — `fetch_mlb_odds()` now falls back to persisted `probable_pitchers` snapshots and synthesizes game context when live Odds API coverage absent; lineup APIs no longer return blanket `implied_runs=4.5`, `park_factor=1.0`, `has_game=false`
- `backend/fantasy_baseball/smart_lineup_selector.py` — `solve_smart_lineup()` now returns `positions`; both lineup routes stop emitting blanket `position="?"`
- `backend/routers/fantasy.py` — Admin endpoints hardened: `/admin/odds-monitor/status` degrades cleanly without Odds API config, `/admin/yahoo/test` emits `connected: true`, `/admin/audit-tables` returns `tables` field

### Validation
- py_compile: clean
- pytest targeted: lineup + admin tests passing

### Root Causes Fixed
1. **Lineup game context missing** — no fallback when Odds API coverage absent; now uses persisted probable-pitcher snapshots
2. **Position data missing** — smart lineup solver did not return `positions` array
3. **Admin endpoint fragility** — endpoints failed without graceful degradation when optional configs missing

### Status
All fixes committed and deployed to Railway.

---

## Phase 2: Postman P0/P1 + Post-Deploy UAT v5 Remediation (Evening Session)

### Commits
None (uncommitted — ready for Wave 2 deploy)

### Files Changed
- `backend/routers/fantasy.py` — Hoisted `player_mapper` imports to module-top so ImportError fails at deploy time, not per-request
- `backend/fantasy_baseball/daily_briefing.py` — Briefing now prefers `opposing_pitcher.team` over scheduler fallback and routes no-game players to MONITOR; contradictory START+"no game today" alerts eliminated
- `backend/fantasy_baseball/smart_lineup_selector.py` — Warning reworded from "Starting but no game today" to "No game scheduled — monitor before lock"
- `backend/services/waiver_edge_detector.py` — Added MCMC gate: when simulator ran (`mcmc_enabled=True`) with `win_prob_gain < 0`, candidate is skipped
- `backend/services/waiver_edge_detector.py` — `_to_waiver_player()` now drops any key still all-digits after `sid_map.get(k, k)`; canonical stat_contract v2 codes (K_P, K_B, K_9, NSV, QS, OBP, ERA, WHIP, IP) preserved
- **NEW:** `tests/test_waiver_recommendations_gates.py` — MCMC negative-gain gate regression test
- **NEW:** `tests/test_daily_briefing_no_game_contract.py` — Briefing contract regression test
- **NEW:** `tests/test_roster_waiver_enrichment_contract.py` — Roster/waiver enrichment tests (5 regression tests)

### Validation
- py_compile: clean
- pytest targeted (72 fantasy tests): **72 passed**
- pytest full suite: **309 passed / 0 regressions**

### Root Causes Fixed (Postman P0/P1)
1. **Roster ImportError** — `player_mapper` imports triggered per-request failures; hoisted to module-top for deploy-time failure
2. **Briefing opponent "TBD"** — scheduler fallback lacked team resolution; now prefers `opposing_pitcher.team`
3. **Waiver MCMC negative gain** — no gate prevented negative `win_prob_gain` recommendations from passing through
4. **Waiver stat-contract leak** — bare numeric Yahoo stat_ids (e.g., "38") leaked into API output; now filtered

### Root Causes Fixed (Post-Deploy UAT v5)
5. **Roster enrichment null (P0)** — Canonical roster handler never called `get_players_stats_batch()` after `get_roster()`, so player_mapper received empty `stats` dicts and `season_stats` stayed null for every player. **Fixed:** Added batch hydration call.
6. **Waiver `matchup_opponent = "TBD"` (P0)** — Inline scoreboard parser did only 2-level descent; Yahoo's payload nests `team_key` and `team_stats` one level deeper. **Fixed:** Extracted recursive walker from matchup endpoint into shared helper; both handlers now use identical parsing.
7. **Waiver `category_deficits = []` (P0)** — Cascading consequence of matchup issue; deficit block gated on `matchup_opponent != "TBD"`. **Fixed:** Same helper now feeds both opponent resolution and deficit math in single scoreboard fetch.

### UAT v5 Results (Post-Deploy Validation)
**95 PASS / 3 FAIL / 1 WARN**  
The three FAIL rows (roster enrichment null, waiver matchup="TBD", waiver deficits=[]) are **fixed locally and uncommitted**.

### Status
All fixes **local/uncommitted**. Ready for Wave 2 deploy. Targeted regression sweep green. No pre-existing tests broken.
