# Agent Task Handoff

## Task ID
**t_b9c63f54** — BDL #2: Player search auto-heal unmapped Yahoo players (EV 4.67)

## Agent
**claude**

## Objective
Fix the new call-up blind spot that blocks ALL downstream enrichment. When a player gets called up from minors, Yahoo assigns them a player_key but our BDL player search pipeline cannot map them because they are not in the existing player_id_mapping table. This suppresses every BDL/Statcast feature for new call-ups.

## Context
- **Project**: CBB Edge — Fantasy baseball (MLB)
- **Branch**: agent/claude/bdl-2-player-search
- **Worktree**: /mnt/c/Users/sfgra/repos/Fixed/cbb-edge-claude
- **Hermes session**: 2026-05-15 — BDL opportunity analysis

## Problem
New MLB call-ups appear in Yahoo with valid player_keys, but our pipeline fails to enrich them because:
1. player_id_mapping table does not have the new player yet
2. BDL player search is not triggered as a fallback
3. All downstream features (ownership %, Statcast, projections) return null/empty
4. User sees "— owned" and missing stats for new call-ups

## Files to Modify
-  — add auto-heal logic (likely needs to be created or modified)
-  — where roster parsing happens, trigger search on unmapped players
-  — player_id_mapping table schema (may need new columns for search metadata)
-  — where player data is ingested, hook in auto-heal

## Existing Patterns (from Hermes skill)
1. **Silent failure guard**: Never silently return empty — always log + attempt fallback
2. **Yahoo shape variance**: Handle missing/optional fields gracefully
3. **ID mapping**: player_id_mapping is the canonical bridge between Yahoo IDs and our internal IDs

## Solution Approach
1. When yahoo_client_resilient.py encounters a player_key not in player_id_mapping:
   - Trigger BDL player search API with player name + team
   - Store the mapping in player_id_mapping with a  flag
   - Set a TTL (e.g., 7 days) for auto-healed mappings to allow manual override
2. On daily ingestion, scan for unmapped Yahoo players and batch-heal them
3. Log all auto-heal events for monitoring

## Tests Required
1. Test auto-heal for a known unmapped player (mock BDL search response)
2. Test that auto-healed mapping is persisted in player_id_mapping
3. Test that downstream enrichment works after auto-heal (ownership % populated)
4. Test TTL expiration (old auto-heal mappings get refreshed)
5. Test batch-heal during daily ingestion

## Acceptance Criteria
- [ ] New call-ups show ownership % within 24 hours of first Yahoo appearance
- [ ] Auto-heal events are logged and countable
- [ ] No regression in existing player mapping
- [ ] All tests pass
- [ ] Handoff summary written to .agent-handoffs/COMPLETED_t_b9c63f54.md

## Hermes Context
From earlier analysis: "Do player search before injuries — unresolved Yahoo players suppress every BDL/Statcast feature for new call-ups, so fixing ID mapping unlocks a multiplier on everything else."

The ownership bug (showing "— owned") was a deploy gap, not a code bug. This task fixes the root cause for NEW players.

## Commands to Run


## Notes
- BDL = Baseball Data Labs (our data provider)
- player_id_mapping table bridges Yahoo player_key ↔ our internal player_id
- This is high-priority because it unlocks BDL #1 (streaming signal) and BDL #3 (injury overlay)
