name: Deploy to Railway

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov
    
    - name: Create test database
      run: |
        PGPASSWORD=postgres psql -h 127.0.0.1 -U postgres -c "CREATE DATABASE test_cbb_edge;"

    - name: Set up test database
      env:
        DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/test_cbb_edge
      run: |
        python scripts/init_db.py

    - name: Syntax check core files
      run: |
        python -m py_compile backend/main.py backend/betting_model.py backend/services/analysis.py backend/services/odds.py

    - name: Run tests
      env:
        DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/test_cbb_edge
        API_KEY_USER1: test_key_12345678901234567890
        ENVIRONMENT: development
      run: |
        pytest tests/ -v --cov=backend

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Install Railway CLI
      run: npm install -g @railway/cli
    
    - name: Deploy to Railway
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      run: railway up
